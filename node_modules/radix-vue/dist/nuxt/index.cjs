'use strict';

const kit = require('@nuxt/kit');
const components = require('radix-vue');

function _interopNamespaceCompat(e) {
  if (e && typeof e === 'object' && 'default' in e) return e;
  const n = Object.create(null);
  if (e) {
    for (const k in e) {
      n[k] = e[k];
    }
  }
  n.default = e;
  return n;
}

const components__namespace = /*#__PURE__*/_interopNamespaceCompat(components);

const index = kit.defineNuxtModule({
  meta: {
    name: "@radix-vue/nuxt",
    configKey: "radix",
    compatibility: {
      nuxt: "^3.0.0"
    }
  },
  defaults: {
    prefix: ""
  },
  setup(options) {
    for (const component of Object.keys(components__namespace)) {
      kit.addComponent({
        name: `${options.prefix}${component}`,
        export: component,
        filePath: "radix-vue"
      });
    }
  }
});

module.exports = index;
